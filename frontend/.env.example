NEXT_PUBLIC_APP_NAME=EduLMS
# Used for SSR / server-side fetches. Browsers use same-origin /api-proxy/v1 instead.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Optional: Laravel origin for Next.js /api-proxy rewrites (defaults by NODE_ENV)
# API_PROXY_ORIGIN=http://127.0.0.1:8000
