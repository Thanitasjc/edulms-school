import ModuleReadyState from "@/components/feedback/module-ready-state";

export default function UsersPage() {
  return (
    <ModuleReadyState
      title="Users"
      description="User CRUD UI is wired to /api/v1/users. Full data-table screens will be completed in the next feature pass."
    />
  );
}
